import 'club.dart';
import 'person.dart';

class Database {
  // Club a = Club("Name", "description", [], "city", "state", "country");
  // Person b = Person("First", "Last", "DOB", [], []);
  // a.addMember(b);
  // b.addClub(a);
  // b.addFriend(c);

}