import 'club.dart';

class Person {
  String firstName;
  String lastName;
  String dob;
  List<Person> friends;
  List<Club> clubs;

  Person(this.firstName, this.lastName, this.dob, this.friends, this.clubs);

  // void add
}